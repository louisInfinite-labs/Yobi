namespace Yobi.Domain.Entities
{
    // DesktopMate: small, borderless, transparent, always-on-top - just the character floating
    // over the desktop. Room: a normal-sized, resizable, opaque window showing the character in
    // a customizable background scene with surrounding UI panels.
    public enum CompanionMode
    {
        DesktopMate,
        Room
    }
}
